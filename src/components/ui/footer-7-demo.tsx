import { Footer7 } from "@/components/ui/footer-7";

/** Example: generic block defaults (swap for `amalFooter7Defaults` in the site footer). */
const DemoOne = () => {
  return <Footer7 />;
};

export { DemoOne };
