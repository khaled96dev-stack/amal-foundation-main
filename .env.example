# Production canonical URL — set the same value in Vercel → Settings → Environment Variables (Production).
NEXT_PUBLIC_SITE_URL=https://www.amalfoundation.org

# Optional: Google Search Console → verify → "HTML tag" → paste the content value only (not the whole meta tag).
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

NEXT_PUBLIC_FACEBOOK_URL=
NEXT_PUBLIC_INSTAGRAM_URL=
NEXT_PUBLIC_TIKTOK_URL=
